import { HighlightKeyPipe } from './highlight-key.pipe'

describe('HighlightKeyPipe', () => {
	it('create an instance', () => {
		const pipe = new HighlightKeyPipe()
		expect(pipe).toBeTruthy()
	})
})
