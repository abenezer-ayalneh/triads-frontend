import { AutoCapitalize } from './auto-capitalize'

describe('AutoCapitalize', () => {
	it('should create an instance', () => {
		const directive = new AutoCapitalize()
		expect(directive).toBeTruthy()
	})
})
